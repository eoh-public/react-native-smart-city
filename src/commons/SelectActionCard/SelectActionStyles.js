import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapcontent: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  card: {
    marginTop: 16,
    marginBottom: 0,
  },
  mt4: {
    marginTop: 4,
  },
});
